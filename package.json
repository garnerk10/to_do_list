{
  "name": "to_do_list",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "scripts": {
    "watch": "webpack --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "webpack serve --open",
    "build": "webpack",
    "deploy": "npm run build && gh-pages -d dist",
    "gh-deploy-init": "git push origin && git subtree push --prefix dist origin gh-pages",
    "gh-deploy": "git push origin :gh-pages && git subtree push --prefix dist origin gh-pages"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/garnerk10/to_do_list.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/garnerk10/to_do_list/issues"
  },
  "homepage": "https://github.com/garnerk10/t0_do_list#readme",
  "devDependencies": {
    "css-loader": "^6.7.4",
    "gh-pages": "^5.0.0",
    "html-webpack-plugin": "^5.5.1",
    "style-loader": "^3.3.3",
    "webpack": "^5.84.0",
    "webpack-cli": "^5.1.1",
    "webpack-dev-server": "^4.15.0"
  },
  "dependencies": {
    "date-fns": "^2.30.0"
  }
}
